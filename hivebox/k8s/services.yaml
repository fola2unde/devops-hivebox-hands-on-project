# k8s/service.yaml
   apiVersion: v1
   kind: Service
   metadata:
     name: hivebox-service
     namespace: default
     labels:
       app: hivebox
   spec:
     type: ClusterIP
     ports:
     - port: 80
       targetPort: 5000
       protocol: TCP
       name: http
     selector:
       app: hivebox